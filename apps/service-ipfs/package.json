{
  "name": "@ssi/service-ipfs",
  "version": "0.1.0",
  "description": "Encrypted IPFS pinning service for Self-Sovereign Identity off-chain data.",
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "tsx watch src/index.ts",
    "start": "node dist/index.js",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "clean": "rimraf dist"
  },
  "dependencies": {
    "fastify": "^4.27.0",
    "helia": "^4.2.1",
    "@helia/json": "^3.0.1",
    "@libp2p/peer-id-factory": "^4.0.3"
  },
  "devDependencies": {
    "@types/node": "^20.11.0",
    "tsx": "^4.7.0",
    "typescript": "^5.4.5",
    "vitest": "^1.6.0"
  }
}
