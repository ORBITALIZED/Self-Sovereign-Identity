{
  "name": "@ssi/zk-circuits",
  "version": "0.1.0",
  "private": true,
  "description": "Zero-knowledge circuits (Circom) for selective-disclosure credential proofs.",
  "scripts": {
    "compile": "bash scripts/compile.sh",
    "setup": "bash scripts/setup.sh",
    "pretest": "bash scripts/compile.sh 2>/dev/null; true",
    "test": "node test/credential.test.js && node test/age.test.js",
    "clean": "rimraf build keys"
  },
  "devDependencies": {
    "circom_tester": "^0.0.24",
    "snarkjs": "^0.7.4",
    "rimraf": "^5.0.5"
  }
}
